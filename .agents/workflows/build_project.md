---
description: How to install dependencies, compile, and build the project.
---
# Build Project Workflow

This workflow will prepare the Next.js application by installing dependencies and building the production output.

1. Ensure dependencies are installed by running:
// turbo
```bash
npm install
```

2. Compile and build the Next.js project:
// turbo
```bash
npm run build
```
